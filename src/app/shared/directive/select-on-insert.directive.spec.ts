import { SelectOnInsertDirective } from './select-on-insert.directive';

describe('SelectOnInsertDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectOnInsertDirective();
    expect(directive).toBeTruthy();
  });
});
